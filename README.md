# Forte for Claude Code

Keeps Claude Code accurate on Forte's CLI, SDK, auth, and deployment workflows.

```
/plugin marketplace add forteplatforms/claude-plugin
/plugin install claude-plugin@forteplatforms
```

Claude uses it automatically when you work with Forte, and updates it on its own as Forte changes.
